{
  "name": "unit-tests-tech-review",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "jest --outputFile=logs.json --json --verbose=false",
    "test_dev": "jest --outputFile=logs.json --json --verbose=true -o"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^20.0.4"
  },
  "dependencies": {
    "express": "^4.15.3"
  }
}
